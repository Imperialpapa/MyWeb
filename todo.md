# 할 일

## 바로 할 것
- [x] **분야 id 이관 — 스키마 반영 완료** (2026-09-10). 분야를 이름이 아니라 id 로 잇게 바꿨다.
  이제 분야 이름을 바꿔도 자료·담당자 지정·권한이 따라온다. 분야 편집은 텍스트 상자에서 **행 편집기**로 바뀌었다.
  SQL Editor 에서 `supabase/schema.sql` 전체 실행을 마쳤고, 아래를 눈으로 확인했다.
  - 분야 4개가 id(`dev`·`life`·`ai`·`humor`)를 받았다
  - 자료 75개가 전부 분야를 이었다 (못 이은 자료 0, 이름·id 어긋남 0)
  - 트리거 3개(`settings_keep_ids`·`settings_sync_cat_names`·`items_resolve_category`)가 붙었고, 옛 함수 `manages_category` 는 사라졌다

  **겪은 함정 — 스키마를 다시 손댈 사람이 반드시 알아야 한다.**
  첫 시도가 `42703: column "category_id" does not exist` 로 통째로 롤백됐다.
  `manages_item` 이 `language sql` 함수인데, 이 종류는 **만드는 시점에** 본문의 컬럼이 있는지 검사한다.
  그 컬럼을 만드는 `alter table` 이 함수보다 아래에 있어서 걸렸다. 분야 주 관리자 함수 3개를
  테이블 정의 뒤로 옮겨 고쳤다. `plpgsql` 은 본문을 검사하지 않으므로 트리거 셋은 파일 어디에 있어도 된다.
  그러니 `language sql` 함수는 항상 자기가 읽는 컬럼보다 **아래**에 두어야 한다.

- [x] **배포 세 단계 완료** (2026-09-10). 스키마 → 함수 → 화면 순서를 지켰다.
  1. SQL Editor 에서 `schema.sql` 실행 (트리거 3개, `v=2`, 분야 못 이은 자료 0건 확인)
  2. `npx supabase functions deploy ai`
  3. `git push` → 커밋 `e9e2815`, 배포본 지문 `1cac423`

- [ ] **배포 뒤 확인 두 가지.**
  **다음에 코드를 손대기 전에 이것부터 한다** (2026-09-10 에 그렇게 정했다).
  먼저 보지 않으면 나중에 문제가 났을 때 이관 탓인지 새 작업 탓인지 가릴 수가 없다.
  1. **분야 편집에서 이름을 하나 바꿔 보기.** 그 분야 자료가 따라오고 담당자 지정이 남아 있어야 한다.
     이번 작업 전체가 이걸 위한 것이다. 확인한 뒤 이름은 되돌려도 된다
  2. **바닥글에 `제휴 사이트: ant@IT ↗` 가 뜨는지** (제휴 작업의 마지막 한 칸)
  3. ⋯ 더 보기의 `배포본` 이 **`1070a3c`** 인지 확인 (Vercel 배포가 끝났는가).
     제휴 작업으로 `index.html` 이 또 바뀌어 지문이 `1cac423` → `1070a3c` 가 됐다

  배포됐는지 보는 법: ⋯ 더 보기의 `배포본` 지문을 **커밋된 내용**의 md5 앞 7자리와 견준다.
  ```bash
  git show HEAD:index.html | md5sum   # 앞 7자리가 화면의 "배포본" 과 같으면 반영된 것
  ```
  **작업 폴더 파일에 `md5sum index.html` 을 걸면 안 된다.** 이 저장소는 `core.autocrlf=true` 라
  새로 받은 작업 폴더의 `index.html` 은 CRLF 인데, Vercel 이 내려 주는(= 저장소에 든) 내용은 LF 다. 지문이 영영 다르다.
  게다가 작업 폴더 파일에는 아직 안 올린 수정이 섞여 있어 "무엇이 배포됐는가" 의 답이 못 된다.

  반영 뒤 **분야 편집에서 이름을 하나 바꿔 보고** 그 분야 자료가 그대로 따라오는지, 담당자 지정이 남아 있는지 확인할 것.

  다시 확인할 일이 생기면 쓰는 쿼리:
  ```sql
  -- 트리거 3개
  select tgname from pg_trigger where tgname in ('settings_keep_ids','settings_sync_cat_names','items_resolve_category');
  -- 분야마다 id 가 붙었는가
  select jsonb_pretty(value->'list') from public.settings where key='categories';
  -- 분야를 못 이은 자료가 남았는가. 분야를 비워 둔 자료가 없다면 0 이다.
  -- 0 이 아니어도 자료를 잃은 것은 아니다 — 그만큼이 화면 왼쪽 "분야 없음" 에 모여 거기서 고칠 수 있다.
  select count(*) from public.items where category_id is null;
  -- 이름과 id 가 어긋난 자료 (항상 0 이어야 한다)
  -- `category_id is not null` 이 있어야 한다. id 가 없고 이름 사본만 남은 자료는 바로 위 쿼리가 세는
  -- "분야 없음" 이지 어긋난 자료가 아닌데, 빼지 않으면 그걸 여기서 또 세어 멀쩡한 DB 가 고장 나 보인다.
  select count(*) from public.items i
   where i.category <> '' and i.category_id is not null and not exists (
     select 1 from public.settings s, lateral jsonb_array_elements(s.value->'list') c
      where s.key='categories' and c->>'id' = i.category_id and c->>'name' = i.category);
  ```

- [ ] **되돌리기 절차** (위가 잘못됐을 때). 순서는 반대다 — 화면 → 함수 → 스키마.
  자료는 안 지워진다. `items.category`(이름)가 그대로 남아 있어 옛 코드가 그대로 읽는다.

  **옛 `schema.sql` 을 먼저 통째로 실행하고, 트리거는 그다음에 지운다.** 순서를 뒤집지 마라.
  트리거를 먼저 지우면 정책은 아직 id 를 보는데(`manages_category_id`) `settings_keep_ids` 만 사라진다.
  그 사이 id 를 안 실어 보내는 저장(옛 탭·옛 화면)이 **한 번만** 들어와도 `settings.categories` 의 id 가 통째로 지워지고,
  `manages_category_id` 가 아무것도 못 찾아 **분야 주 관리자 전원이 오류 하나 없이 권한을 잃는다.**
  `items.category_id` 도 목록에 없는 id 를 가리키게 된다. 아무도 눈치채지 못하는 종류의 사고다.
  옛 `schema.sql` 을 먼저 돌리면 정책이 이름 기준으로 돌아가므로 그 구멍이 아예 안 생긴다.
  ```sql
  -- 1) 옛 schema.sql 전체를 SQL Editor 에서 실행 (manages_category 와 옛 정책이 돌아온다)
  -- 2) 권한이 돌아온 것을 확인한 뒤에 새 트리거 셋을 지운다
  drop trigger if exists settings_keep_ids on public.settings;
  drop trigger if exists settings_sync_cat_names on public.settings;
  drop trigger if exists items_resolve_category on public.items;
  ```
  트리거를 남겨 두면 옛 화면으로 돌아가도 분야 개명이 계속 막히므로, 지우는 것 자체는 필요하다.
  `items.category_id` 컬럼은 **지우지 마라** — 다시 이관할 때 그대로 쓴다.

  **되돌린 동안에는 분야 이름을 절대 바꾸지 마라.** 옛 화면은 id 라는 것을 모르고 이름만 저장한다.
  그래서 다시 이관할 때 `schema.sql` 이관 (1)이 새 이름에 **새 id** 를 발급하고, 옛 이름을 그대로 든 자료는
  그 분야로 못 붙는다. 결국 같은 분야가 둘로 갈라지고, 담당자 지정은 자료가 없는 빈 쪽에 남는다.
  이름을 바꿔야 한다면 되돌리기를 풀고 새 화면에서 바꿔라.
  같은 이유로 그 기간에 새로 만든 분야도 id 를 못 받으니, 다시 이관한 뒤 분야 편집을 한 번 열어 저장해 두면 깔끔하다.

- [x] **제휴 사이트: 모음으로 관리하고 바닥글에 붙이기** (2026-09-10 완료).
  반영 확인: 모음 정리 트리거 있음, 모음 `partners` 주인 없음, 자료 담김, 제휴 자료에 분야·주인 없음.
  배포된 `config.js` 에 `PARTNER_COL:"partners"` 실린 것까지 확인. 배포본 지문 `1070a3c`.
  **남은 것은 눈으로 보는 것 하나** — 사이트 맨 아래에 `제휴 사이트: ant@IT ↗` 가 뜨는지.

  첫 상대는 **ant@IT** (`https://gameant.pages.dev`) — IT 정보·개발 지식·코딩·AI 를 다루는 한국어 사이트.
  이 저장소 어디에도 기록이 없어 그동안 반영되지 않았다. 그래서 먼저 여기에 적는다.

  **새 표를 만들지 않는다.** 모음(collections)이 이미 "주제별로 항목을 묶는" 그릇이고
  자료·검색·권한·신고가 전부 붙어 있다. 제휴 사이트도 그냥 자료 하나이고, 모음 하나에 담기면 된다.
  1. 모음 `partners`(제휴 사이트)를 만들고 거기에 제휴 사이트를 자료로 담는다 (`supabase/partners.sql`)
  2. `config.js` 의 `PARTNER_COL` 이 그 **모음 id** 를 가리킨다. 빈 문자열이면 바닥글에 안 나온다
  3. 바닥글이 그 모음의 자료를 링크로 그린다

  **모음을 이름이 아니라 id 로 가리킨다.** 오늘 분야에서 겪은 그대로다 —
  이름으로 가리키면 모음 이름을 바꾸는 순간 바닥글이 조용히 빈다.
  제휴가 늘면 `partners.sql` 에 자료를 한 줄 더하고 모음에 담기만 하면 된다. 코드는 안 고친다.

  **검토에서 나온 것 — 고쳤지만 반영이 남았다.** 모음 id 와 자료 id 는 **선착순**이다.
  그 자리가 비어 있으면 아무 로그인 사용자나 JSON 가져오기로 같은 id 를 만들어
  사이트 전체 바닥글에 자기 링크를 띄울 수 있었다. 세 겹으로 막았다.
  - 바닥글은 **주인 없는 모음만** 그린다 (`partners.sql` 로 만든 것만 주인이 없다)
  - 자료를 지우면 모음에서도 빠진다 (`schema.sql` 새 트리거). 안 그러면 지운 자리가 예약석으로 남는다
  - `partners.sql` 은 남이 그 id 를 차지하고 있으면 시끄럽게 멈춘다. 남의 모음에 우리 자료를 담아 주지 않는다
  제휴 자료는 **분야를 일부러 비웠다.** 분야를 주면 그 분야 담당자가 바닥글 링크의 주소를 바꿀 수 있다.

  반영 순서 (스키마가 먼저):
  1. SQL Editor 에 `supabase/schema.sql` 다시 붙여넣기 (모음 정리 트리거)
  2. SQL Editor 에 `supabase/partners.sql` 다시 붙여넣기 (제휴 자료의 분야를 비운다)
  3. `git push` 는 이미 됨 — 바닥글이 나오는지 확인

- [x] **AI 기능 배포 완료** (2026-09-07). Edge Function `ai` 배포, `ANTHROPIC_API_KEY` 등록, `schema.sql` 재실행 완료
- [x] **AI 분류 제안 동작 확인 완료** (2026-09-07)
- [ ] **Anthropic 키 교체**: 지금 키는 2026-10-07 만료. 만료는 생성 시에만 정해지고 연장이 안 된다.
  콘솔에서 **만료 Never** 로 새 키를 만들고 `npx supabase secrets set --project-ref csxndscngmkciibarumi "ANTHROPIC_API_KEY=새키"` 로 교체한 뒤 옛 키 삭제.
  함수 재배포는 필요 없다. 만료 7일 전·1일 전 안내 메일이 온다. (자동 갱신은 불가능 — 키 생성 API 가 없고 개인 계정은 관리용 API 자체를 못 쓴다)
- [x] **`schema.sql` 재실행 + 함수 재배포 완료** (2026-09-09).
  `ai_take_quota(lim, cost)`·`ai_refund_quota(cost)`·`mark_checked(ids)`·`items.checked_at` 반영 확인,
  세 함수 모두 비로그인 호출은 42501 로 막히는 것까지 확인.
  **순서 주의**: 함수를 먼저 배포하면 DB 에 옛 함수뿐이라 **✦ AI 분류 제안까지 전부** 죽는다. 스키마가 먼저다.
  (이번에 실제로 겪었다. 예전 내용이 든 SQL Editor 탭을 그대로 실행해 스키마만 옛것으로 남았고,
  그 사이 AI 기능 전체가 "사용량을 확인하지 못했습니다" 였다. 실행 뒤에는 아래 쿼리로 확인할 것)
  ```sql
  select proname, pronargs from pg_proc
   where pronamespace = 'public'::regnamespace
     and proname in ('ai_take_quota','ai_refund_quota','mark_checked');
  ```
  새로 들어간 것: 주 관리자 권한 함수·정책, `items.checked_at` 컬럼, 가중치를 받는 `ai_take_quota(lim, cost)`·`ai_refund_quota(cost)`,
  점검 시각을 찍는 `mark_checked(ids)`. 옛 `ai_take_quota(integer)` 는 스크립트가 알아서 지운다.
  ```bash
  npx supabase functions deploy ai
  ```
  실행 뒤 확인: `select proname, pronargs from pg_proc where proname in ('ai_take_quota','ai_refund_quota','mark_checked');` 이 3줄이면 정상.
- [x] **Anthropic 콘솔 월 지출 한도 설정 완료** (2026-09-09). 사이트 안의 하루 한도는 우리 쪽 방어선이고,
  이건 그 바깥의 계정 상한이다. 코드에 구멍이 있어도 청구서가 이 위로는 안 올라간다
- [x] **운영비 방어 배포 완료** (2026-09-09). 사용량 가중치 재조정(재분류 1→9, 분야 구조 2→7, 자료 채우기 5→15),
  하루 한도 하향(운영자 600→300, 사용자 40→20), 호출별 토큰 기록, 150초 시간 상한.
  함수 재배포와 푸시까지 끝났고 배포본 지문 `adaf49a` 로 반영 확인
- [x] **분야에 자료 채우기 기능 완성·배포** (2026-09-09). 서버가 링크 생존·중복·위험 명령을 검증하고
  운영자가 카드에서 골라 추가한다. 세 차례 검증(에이전트 42개)에서 확인된 지적 22건을 고쳤다
- [x] **배포본 지문 표시** (2026-09-09). ⋯ 더 보기 아래에 `배포본 adaf49a` 가 나온다.
  로컬에서 `git show HEAD:index.html | md5sum` 앞 7자리와 견주면 배포 여부를 바로 알 수 있다.
  작업 폴더 파일(`md5sum index.html`)로 견주면 안 된다 — `core.autocrlf=true` 라 작업 폴더는 CRLF,
  배포되는 저장소 내용은 LF 여서 지문이 다르고, 아직 안 올린 수정까지 섞인다.
  (오늘 "올렸는데 화면에 안 보인다" 를 겪고 붙였다. 원인은 푸시를 안 한 것이었다)
- [ ] **한 달 뒤 토큰 로그로 가중치 다시 맞추기**. 대시보드 Edge Functions → ai → Logs 에 호출마다
  `{"ai":"expand","in":...,"out":...,"ms":...}` 가 남는다. 지금 `ACTION_COST` 는 추정치 기반이라
  실측이 두 배까지 다를 수 있다. 한 달치가 쌓이면 다시 계산할 것
- [ ] **Supabase 자체 백업**. 무료 요금제는 백업이 없다. GitHub Actions 로 주 1회 `pg_dump` 를 떠 두면 0원이다.
  남이 올린 자료를 잃으면 복구 수단이 전혀 없다. 지금 규모에서 가장 값싼 보험이다
- [ ] **일시정지 방지**. Supabase 무료 프로젝트는 일주일 쉬면 멈춘다. 하루 1회 가벼운 질의를 거는 cron 을 걸 것
- [ ] **도메인 확보 검토** (연 15,000원 안팎). 지금 `*.vercel.app` 에 묶여 있으면 호스팅을 옮길 때마다
  Supabase Redirect URLs 와 카카오 콘솔 도메인을 다시 설정해야 한다. 도메인을 먼저 붙이면 그 작업이 한 번으로 끝난다
- [ ] **Vercel 무료는 비상업 전용이다.** 결제·광고·유료 플랜을 붙이는 날 Pro(월 3만원대)로 올리거나
  Cloudflare Pages 로 옮겨야 한다. 후원(Donation)은 명시적으로 허용된다. 옮긴다면 도메인을 먼저 붙여 둘 것
- [ ] 운영자로 **✦ AI 정리** 세 섹션을 한 번씩 눌러 보기 (재분류 표, 분야 구조 개편안, **분야에 자료 채우기**)
  (함수 코드를 고치면 `npx supabase functions deploy ai` 만 다시 실행. 로그는 대시보드 Edge Functions → ai → Logs)
- [ ] **자료 채우기 첫 실전**: 자료가 적은 분야를 골라 5개로 한 번 돌려 보고, 링크 배지("정상/없음/확인 불가")가
  실제 상태와 맞는지, 중복 판정이 맞는지 확인. 메모·코드는 내용을 직접 읽고 고를 것
- [x] **운영자 지정 완료**. ✦ AI 버튼은 운영자에게만 보이는데 실제로 눌러 쓰고 있으므로 확인됐다.
  다른 사람을 운영자로 만들 때 쓸 SQL 은 아래에 남겨 둔다
  ```sql
  update public.profiles set is_admin = true
    where id = (select id from auth.users where email = '카카오 계정 이메일');
  ```
- [ ] **Supabase 이메일 템플릿**에 `{{ .Token }}` 넣기 (README 3단계). 안 넣으면 메일에 6자리 코드가 안 실림
- [x] Supabase **URL Configuration** 확인됨. 카카오 로그인이 배포 주소에서 끝까지 도는 것이 증거다.
  도메인을 붙이면 이 값과 카카오 콘솔 플랫폼 도메인을 함께 갱신해야 한다
- [ ] 팀원 2~3명에게 공개 주소 공유해서 카카오 로그인·올리기·검색 한 번씩 해 보게 하기
- [x] **`seed-4.sql`(유머 자료 12건) 반영 확인** (2026-09-10).
  `select count(*) from public.items where id like 'seed4-%'` 가 12 였다. 분야 4개, 자료 75개.
  seed 파일은 그 뒤에 두 군데를 고쳤다 — 중복 판정을 이름이 아니라 **id 기준**으로 바꿨고,
  파일 끝에 "분야 잇기" 한 문장을 넣었다. 분야를 개명한 DB 에서 처음 돌려도 자료가 `humor` 에 붙는다.
  그전에는 이름이 안 맞아 분야 추가를 건너뛴 채 자료만 들어가 전부 "분야 없음" 으로 떨어졌다.

## 사이트 다듬기
- [ ] `icon.png` 를 파비콘(브라우저 탭)과 카카오톡 링크 미리보기(`og:image`)로 연결.
  지금 파비콘은 `index.html:8` 의 인라인 SVG 이고 `icon.png`(108,776B)는 **어디서도 안 불린다**(확인함).
  **연결하기 전에 먼저 줄일 것** — 아래 "비용 줄이기" 의 같은 항목을 보라
- [ ] 카카오톡 공유 시 제목·설명 미리보기 문구 정하기 (`og:title`, `og:description`)
- [ ] 모바일에서 종류·분야 필터도 너무 길면 접기 검토
- [ ] 첫 방문자용 짧은 안내 (무엇을 올리는 곳인지 한 줄, 로그인은 카카오 한 번)
- [ ] 로그인 창에서 이메일 방식은 "다른 방법으로 로그인" 아래에 숨길지 검토

## 로그인·계정
- [ ] **Google 로그인 열기 검토.** 지금은 카카오 전용이라 **해외 사용자는 가입 자체가 안 된다.**
  화면 코드는 이미 다 들어 있다(버튼·아이콘·처리 함수, 공급자 무관). Google Cloud OAuth 클라이언트를 만들고
  Supabase 대시보드에서 켠 뒤 `config.js` `AUTH_PROVIDERS` 에 `"google"` 한 줄이면 끝난다. 반나절, 0원.
  밖으로 넓힐 생각이라면 화면 번역보다 이게 100배 싸고 효과가 크다
- [ ] 카카오 앱: 사업자등록번호가 생기면 비즈니스 정보 갱신 → 이메일 **필수 동의** 로 변경 (지금은 선택 동의라 이메일 없는 계정이 생길 수 있음)
- [ ] **카카오 동의항목에서 닉네임을 필수 동의로 변경** (개발자 콘솔 → 카카오 로그인 → 동의항목).
  선택 동의면 사용자가 체크를 빼서 표시 이름 없는 계정이 생긴다. 실제로 하나 생겼다(2026-09-05 가입 계정).
  이름이 없으면 주 관리자 지정도 못 하고 누가 올렸는지도 모른다. 공개 확장 전에 바꿔 두는 편이 좋다
- [ ] 같은 사람이 카카오·이메일로 각각 가입해 계정이 둘이 되는 경우 대응 (안내 문구 또는 계정 연결).
  지금 운영자 본인이 이미 계정 둘(이메일·카카오)이고 이름도 같아, 주 관리자 지정 목록에 "황제파파"가 두 번 나온다

## 운영
- [ ] **이메일 로그인은 지금도 사실상 못 쓴다.** Supabase 기본 SMTP 는 **시간당 2통** 제한이라
  예비 수단 구실을 못 한다. Resend 무료(월 3,000통·하루 100통)를 붙이면 0원으로 해결된다.
  카카오 로그인이 막히는 날을 대비해 미리 붙여 두는 편이 좋다
- [ ] 도메인 연결 (Vercel Settings → Domains). 연결 후 Supabase `Site URL`, 카카오 플랫폼 도메인도 갱신
- [ ] Supabase 무료 티어 한도(용량·요청) 모니터링, 백업은 `⋯ → 파일로 관리 → JSON 내보내기` 로 주기적으로
- [ ] `gh auth setup-git` 실행해 두기 (푸시 시 자격 증명 창 멈춤 방지)

## 비용 줄이기 (2026-09-09 조사에서 남은 것)

**← 다음 작업은 여기서 시작한다** (2026-09-10 에 그렇게 정했다).
다만 **맨 위 "배포 뒤 확인" 을 먼저 끝내고** 시작할 것. 분야 이름을 하나 바꿔 보는 그 확인이다.
코드를 더 얹기 전에 이관이 실제로 됐는지 눈으로 봐야 한다. 안 그러면 나중에 문제가 생겼을 때
이관 탓인지 새 작업 탓인지 가릴 수가 없다.

> **2026-09-11 조사에서 이 절의 앞 세 항목이 틀린 것으로 드러났다.** 에이전트 4개로 실제 코드를 읽고 숫자를 다시 셌다.
> - **둘 다 돈을 한 푼도 안 줄인다.** Supabase 전송량은 무료 한도 5GB 의 **1% 남짓**을 쓰고 있다.
>   실제로 돈이 나가는 곳은 여전히 Claude API 하나뿐이다. 이 둘은 "비용" 이 아니라 **체감 속도·정확성** 항목이다
> - "본문 빼기는 **한 줄이다**" → 거짓. 본문을 읽는 곳이 7군데이고 그중 둘은 **조용한 데이터 손실**이다
> - "쿨다운으로 **2~3배** 준다" → 근거 없음. 요청 수로는 8→3(1.6배)이고, 그나마 쿨다운이 못 막는 3건이 남는다
> - "`icon.png` 가 방문의 **71%**" → 거짓. `index.html` 은 `icon.png` 를 **부르지 않는다**(파비콘은 인라인 SVG다).
>   지금 기여도는 **0%** 다. 줄여도 0원이고 전송량도 안 준다

- [ ] **먼저: 실시간 연결이 죽은 것을 알아채게 고치기** (index.html 한 파일, `git revert` 한 번으로 되돌아감).
  아래 본문·쿨다운 이야기보다 **이게 먼저**다. 지금은 연결이 끊겨도 알약이 초록으로 남고,
  놓친 변경을 메울 길이 탭 복귀 재조회뿐이다. 그 상태에서 쿨다운을 걸면 조용한 거짓말이 된다.
  1. `index.html:680` 에 `CLOSED` 갈래가 없다 → `else setConn('err')` 로 바꿔 SUBSCRIBED 아닌 모든 상태를 빨간불로
  2. `index.html:662` 가 조회 **성공** 때 `setConn('db')` 를 안 부른다 → 한 번 실패하면 알약이 영영 빨갛다
  3. `loadAll` 에 진행 중 가드(inflight)와 `loadAt` 기록 추가. 지금은 빠르게 여러 번 복귀하면 전체 조회가 겹쳐 돈다
  4. 끊겼다 다시 `SUBSCRIBED` 되면 `loadAll()` 한 번 (재연결 메우기).
     **`postgres_changes` 는 재연결해도 놓친 변경을 돌려주지 않는다.** 이게 없으면 탭을 켜 둔 채로 무한히 낡는다
  5. 그다음에야 `index.html:681` 을 **시간이 아니라 연속성**으로 바꾼다 —
     `if(DB_ERR) loadAll(); else if(!liveAt || liveAt>loadAt) loadAll();`. **5분 쿨다운은 넣지 않는다**
  6. `onAuthStateChange` 에서 사용자 id 가 그대로인 `SIGNED_IN`·`TOKEN_REFRESHED` 를 걸러 낸다
     (`SIGNED_OUT` 과 id 변경은 반드시 통과시킬 것 — 여기서 실수하면 로그아웃이 화면에 반영 안 된다).
     요청 수를 실제로 줄이는 것은 이 한 가지뿐이다. supabase-js 가 복귀 때마다 가짜 `SIGNED_IN` 을 쏜다

  여기까지 하고 **멈춰서 배포해 확인한다.** 비행기 모드 5초 뒤 복귀 시 다시 받는지, 알약이 빨개졌다 돌아오는지,
  탭 여러 개를 열어 두고 복귀 1회에 요청이 몇 건 나가는지 (`BroadcastChannel` 때문에 탭 수만큼 늘어난다).

- [ ] **그다음: 목록 질의에서 본문 빼기 — 할지 말지부터 다시 정한다.**
  얻는 것은 조회 1회당 gzip 약 8.4KB 이고 **절감액은 0원**이다. 게다가 검색하는 방문자는 어차피 본문을 다 받게 되므로
  그 사람에게는 절감이 0 이고 첫 검색만 느려진다. 이 서비스의 주 동작이 검색이다.

  하기로 했다면 **본문을 읽는 네 자리**를 먼저 만들어야 한다. 둘은 조용한 데이터 손실이다.
  | 자리 | 지금 빼면 | 어떻게 |
  |---|---|---|
  | 수정 창 | 태그 하나만 고쳐도 **DB 본문이 빈 문자열로 덮인다.** 오류도 백업도 없다 | 열 때 그 항목 본문만 따로 받는다. 못 받으면 **저장을 막는다** |
  | JSON 내보내기 | 본문 없는 백업이 만들어지고, 다시 가져오면 전 자료의 본문이 지워진다 | 내보내기 직전에 `select('id,body')` 한 번. 못 받으면 **내보내기를 막는다** |
  | 상세 펼치기·코드 복사 | 메모·코드 21개(34%)가 빈 카드가 되고 복사가 빈 문자열을 복사한다 | 펼칠 때 그 항목만 받는다 |
  | 검색 | 그 자료 본문에만 있는 토큰이 **81%**. 결과가 0건으로 나올 뿐 표시가 없다 | 첫 타자에 본문을 한 번 받아 캐시 |

  **`body_preview` 생성 컬럼은 쓰지 마라.** `left()` 는 바이트가 아니라 **글자**를 센다.
  본문 중앙값이 61글자라 `left(body,140)` 은 61개 중 0개도 못 자르고, 절감의 79%를 날린다(-65.6% → -14.0%).
  미리보기를 지키면서 절감도 지키는 선택지는 없다. 메모·코드 미리보기 줄을 포기할지 명시적으로 고르고 적을 것.

  **본문 캐시는 `(id, updated_at)` 로 키를 잡고 매 `load('items')` 마다 버린다. 수정 창은 캐시를 절대 보지 않는다.**
  안 그러면 남이 고친 최신 본문을 옛 것으로 덮는다 — 분야를 이름으로 잇다가 겪은 것과 같은 종류의 사고다.
  `owner_id` 를 로그인 여부로 넣었다 뺐다 하는 안은 **이번에 묶지 마라.** 같은 배열이 두 모양을 갖게 된다.
  배포는 스키마 → 화면, 되돌리기는 **화면 → 스키마**.

- [ ] **`icon.png` 는 파비콘·og:image 로 연결하기 *전에* 줄인다** (108KB → 20~30KB).
  지금은 어디서도 안 불러서 전송량이 0 이다. 연결하는 순간 106KB 가 새로 생기므로, 줄이는 것은 그 작업의 일부다
- [ ] **링크 점검을 AI 에서 떼어내기**. 지금은 순수 HTTP 점검이 재분류에 묶여 있어,
  링크만 보고 싶어도 AI 호출 값을 치른다. 점검만 하는 action 을 따로 두면 그 비용이 0이 된다
- [ ] **자료 채우기·분야 구조의 effort 와 상한 낮추기**. 토큰 실측이 한 달쯤 쌓인 뒤에 판단할 것.
  단 `max_tokens` 는 함부로 못 줄인다. opus-5 는 생각 토큰이 이 상한에 함께 들어가서,
  줄이면 드문 손실이 잦은 "응답이 잘렸습니다" 로 바뀐다
- [ ] **분류 제안만 싼 모델로 내리기 검토**. 가장 자주 불리는 기능이라 효과가 크다 (haiku 88%·sonnet 63% 절감).
  ⚠ **haiku-4-5 는 `output_config.effort` 를 거부한다.** 모델 이름만 바꿔 배포하면 분류 제안이 전부 실패한다.
  effort 제거와 함께 배포해야 한다. 분류 자체는 haiku 로 충분하지만 제목·설명 생성이 약한 지점이라,
  절충하려면 sonnet-5. **품질을 눈으로 비교한 뒤에 결정할 것**
- [ ] **실시간 반영을 바뀐 행만으로** (자료 300개 근처에서). 지금은 누가 하나 고칠 때마다
  접속자 전원이 전체 테이블을 다시 받는다. 비로그인 방문자는 아예 구독하지 않게 하는 것도 함께
- [ ] **서버 페이지네이션** (자료 300개 넘을 때). 인덱스는 이미 있다
- [ ] **Cloudflare Pages 이전 검토** (홍보·수익화 직전에). 정적 파일 요청이 무제한이고 비상업 조항이 없다.
  도메인을 먼저 붙여 두면 이전이 설정 한 번으로 끝난다
- [ ] 수익화 1단계는 **후원**으로. Vercel 무료 요금제가 후원은 명시적으로 허용한다. 유료 전환 시점을 미룰 수 있다

**하지 말 것으로 결론 난 것**: Netlify 무료(신규 계정은 배포 20번이면 정지), GitHub Pages(약관이 SaaS 를 명문으로 금지).

## 운영 준비 (법·기록)

- [ ] **개인정보처리방침 만들기**. 수익·사업자등록 여부와 무관하게 이미 의무다(개인정보보호법 제30조).
  카카오 닉네임과 이메일을 받고 있으므로 적용 대상이다. 개인정보보호위원회 표준 양식으로 직접 쓰면 0원
- [ ] **첫 수익이 생기기 전에 국세상담센터(126) 에 사업자등록 필요 여부 확인**.
  무료 서비스가 "사업 개시" 인지에 대한 명확한 유권해석을 못 찾았다. 추측으로 두지 말 것
- [ ] **무료 모니터링 붙이기**: Sentry(에러 월 5,000건 무료) + Umami Cloud(이벤트 월 10만 무료). 둘 다 0원.
  지금은 사용자가 오류를 만나도 알 방법이 없다

## 다음에 볼 것 (공개 확장 전제)
- [ ] **주 관리자 실제 동작 확인**: 팀원 한 명을 어느 분야 담당으로 지정 → 그 사람이 자기 분야 자료를 고치고 신고를 닫을 수 있는지, 다른 분야는 막히는지
- [ ] 담당자가 **자기 닉네임을 바꾸면** 분야 옆 담당자 이름(스냅샷)이 낡는다. 본인이 볼 때는 최신 이름이 나오지만 남에게는 옛 이름이 보인다. 운영자가 지정 화면을 다시 열어 저장하면 갱신됨
- [x] **분야 이름을 바꾸면 담당자 지정이 끊기던 문제 해결** (2026-09-09, 배포는 "바로 할 것" 참고).
  `items.category_id` 가 정본이 되고 `items.category`(이름)는 트리거가 유지하는 표시용 사본이 됐다.
  이름이 키로 쓰이던 약 58곳을 id 로 옮겼다. 고아 하위분야 `건강`·`취미·여행` 은 이관이 settings 에 올려 준다.
  검증에서 나온 조용한 손상 3건도 함께 막았다 — 낡은 이름 한 번에 id 가 지워지던 것,
  새 분야에 자료를 채울 때 자료가 먼저 들어가 고아가 되던 것, JSON 가져오기의 같은 순서 문제.

## 감사에서 남긴 것 (2026-09-07)
- [ ] **성능**: items 가 바뀔 때마다 모든 접속자가 전체 테이블을 다시 받고, 화면도 전부 다시 그린다. 자료가 수백 개를 넘으면 개선 필요 (변경 행만 반영 + 부분 렌더)
- [ ] **1000행 상한**: `.limit(5000)` 은 Supabase API 기본 Max rows(1000)에 조용히 잘린다. 자료가 1000개에 가까워지면 페이지 나눠 읽기 또는 설정 상향
- [ ] **링크 공유**: 항목·모음·검색 결과를 주소로 공유할 수 없다 (URL 라우팅 없음). **공개 확장을 하려면 사실상 필수** — 자료를 찾아도 남에게 보낼 방법이 없다
- [ ] **owner_id 노출**: 비로그인 방문자에게도 각 자료의 owner_id(계정 UUID)가 그대로 내려간다. 공개 서비스에서는 지워서 내보내는 편이 낫다
- [ ] **접근성**: 모달 포커스 가둠·복귀, 토스트 `aria-live`, 힌트 글자 명도 대비, 터치 영역 24px, 검색창 레이블
- [ ] **이름 바꾸기가 과거 글에 반영 안 됨**: `by` 가 항목마다 복제된 스냅샷이라 그렇다. 표시할 때 프로필에서 읽어오는 방식으로 바꿀지 검토
- [x] **모음 고아 id 해결** (2026-09-10). 자료를 지우면 모든 모음의 `item_ids` 에서 그 id 를 빼는
  트리거(`items_strip_from_collections`)를 넣었다. 제휴 바닥글을 만들다 발견했다 — 남은 id 는 그냥 지저분한 것이 아니라
  **예약석**이었다. 자료 id 는 선착순이고 JSON 가져오기가 파일의 id 를 그대로 쓰므로,
  누가 그 id 로 자료를 새로 만들면 남의 모음에 들어앉을 수 있었다
- [ ] **신고 중복·고아**: `reports.item_id` 에 외래키가 없고, 같은 사람이 같은 항목을 여러 번 신고할 수 있다
- [ ] **AI 재분류 표가 모바일에서 읽기 어려움**: 표에 thead/tbody 를 넣어 좁은 화면 눕히기 규칙이 이제 실제로 먹지만,
  칸에 이름표가 없어 값만 쌓인다. 3번 섹션처럼 카드로 바꾸면 깔끔하다
- [ ] **프롬프트 인젝션**: 자료 채우기(expand)의 시스템 프롬프트에 들어가는 남의 글은 `flat()` 으로 한 줄로 눕혔지만,
  organize·suggest 의 **사용자 메시지**는 여전히 원문 그대로다(본문에 줄바꿈이 있어야 분류가 제대로 된다).
  재분류 제안 표가 기본 전체 체크인 것도 그대로. 기본 해제로 바꿀지 검토
- [ ] **AI 정리 3번(자료 채우기)에서 알면서 남긴 것**
  - 위험 명령을 "설명"하는 정상 메모도 `risk=block` 으로 막힌다. 운영자가 새 항목으로 직접 올리면 된다
  - `ai_refund_quota` 는 "지금 날짜" 행을 깎는다. 30~60초짜리 요청이 자정을 넘겨 실패하면 환불이 다음 날로 간다 (드묾)
  - 1번 섹션 라디오 옆 개수는 창을 열 때 센 값이다. 처리 대상은 누를 때 다시 세므로 안전하지만 숫자는 낡을 수 있다
  - IPv6 리터럴 주소(`http://[2606:...]`)는 링크 점검에서 통째로 "확인 불가" 가 된다. 자동으로 버리지는 않는다
- [ ] **스트리밍 전환에서 알면서 남긴 것** (2026-09-09)
  - 스트림 도중 오는 API 오류(과부하·레이트리밋)는 상태 코드 없이 던져져, 문구가 원인과 어긋나고
    SDK 자동 재시도도 걸리지 않는다. `.stream()` + `finalMessage()` 로 바꾸면 함께 해결된다
  - 분류 제안·재분류·분야 구조는 원래 스트리밍이 필요 없었다(10분 거부는 상한 24000 에서만 걸린다).
    지금은 넷 다 같은 경로를 쓰는데, 되돌릴지 그대로 둘지는 실사용을 보고 판단
  - 재분류에서 AI 호출이 실패하면 나란히 돌던 링크 점검이 응답 뒤에도 잠시 살아남는다
- [ ] **카카오 키 파일**(`supabase/톡연계정보.txt`)을 저장소 폴더 밖으로 옮기기. 지금은 `.gitignore` 한 줄이 유일한 방어

## 나중에
- [x] 분야·하위분야 확장 → AI 에이전트 추가 (`seed-3`). 다음 분야는 `add-category` 에이전트로
- [x] Supabase 에 `seed-3.sql` 실행 → AI 에이전트 분야·자료 11개 반영 확인 (총 49개)
- [ ] 자료 품질 관리 규칙: 신고 검토 기준, 중복 링크 처리
- [ ] 아티팩트 버전("도구함")은 조직 내부 전용이므로 정리하거나 링크만 남기기

### 한국어/영어 전환 (2026-09-09 조사, **우선순위 최하로 미룸**)

하기로 결정한 것이 아니라, 나중에 판단할 때 조사를 다시 하지 않으려고 남긴다.

**먼저 답해야 할 질문**: 외국인이 이 사이트에 **한국 자료를 읽으러** 오는가, **자기 자료함을 만들러** 오는가.
- 자기 자료함을 만들러 온다면 → 화면 문구 번역이 곧 완성품이다. 자료 번역은 영영 필요 없을 수도 있다
- 한국 자료를 읽으러 온다면 → 화면 문구만으로는 1mm 도 못 간다. 자료 번역까지 가야 하고 검수할 사람이 없다

**규모 (실측)**
- 화면 문구 **448곳 등장 / 고유 384개**. 서버(Edge Function)가 만들어 화면에 그대로 나오는 한국어 오류 문구가 **42개** 더 있다
- 작업량 추정: 화면 문구 5~7일, 분야 체계 2~3일, 자료 번역 3일

**미리 알아 둘 함정**
- **전역 번역 함수를 `t` 로 지으면 안 된다.** `t` 는 이미 지역 변수·람다 인자로 14곳에서 쓰인다(특히 `row()` 안).
  `T('key',{vars})` 를 쓴다. `T` 는 파일 전체에서 식별자로 쓰인 적이 없다
- **사이드바 분야 이름은 이제 순수한 데이터 문제로 줄었다.** (2026-09-10 id 이관 완료 —
  연결 키는 `id` 이고 이름은 표시용 사본이다. 조사 당시 "이름이 키로 쓰이는 곳 58곳" 은 이제 없다.)
  남은 것은 `settings.categories` 가 이름을 언어별로 갖게 할지 하나뿐이다
- **서버 오류 문구를 문자열이 아니라 코드로 바꿔야 한다.** 안 하면 영어 화면에서 오류만 한국어로 나온다
- **AI 프롬프트가 한국어 결과를 지시한다**(제목·주석). 언어를 함께 보내 갈라야 한다
- **문장은 완결 단위로만 자른다.** 한국어와 영어는 어순이 달라 절 단위로 쪼개면 재조립이 안 된다.
  조사(`를`, `가`)를 하드코딩한 곳도 있다
- **모달 안에 언어 전환 버튼을 두면 안 된다.** 모달은 다시 그릴 수 없어 입력값과 AI 캐시가 날아간다.
  헤더의 ◐ 팝오버에 넣는다. 거기엔 이미 절을 나누고 다시 여는 패턴이 있다
- CSS 가 한국어 조판에 맞춰져 있다(`word-break:keep-all`, 음수 자간, Gothic A1). 영어에서는 갈라야 한다
- 기본 언어는 **ko 고정**. `navigator.language` 자동 전환은 하지 않는다. 영어 브라우저를 쓰는 한국 개발자가 많다
- 안 옮긴 문구를 찾는 법: 가짜 언어 모드(`?lang=zz`)로 번역된 것만 괄호로 감싸면, 괄호 없는 한국어가 곧 빠뜨린 문구다
- 상시 비용을 잊지 말 것. 사전을 도입하면 새 기능마다 영어 문안을 함께 써야 하고,
  안 쓰면 조용히 한국어로 폴백해 반년 뒤 반쯤 섞인 화면이 된다. 정적 점검이 없으면 개발자는 영영 모른다

**자료 번역 비용** (해야 할 경우). 번역은 opus 로 할 일이 아니다. `ask()` 가 모델을 인자로 받게 고치는 것이 가장 값싼 한 줄이다.

| 모델 | 61건 | 500건 | 5,000건 |
|---|---|---|---|
| haiku-4-5 + Batch | 52원 | 420원 | 4,200원 |
| haiku-4-5 | 103원 | 840원 | 8,400원 |
| opus-5 | 1,160원 | 9,500원 | 95,000원 |

**하지 말 것으로 결론 난 것**: 분야 이름을 id 없이 영어로 바꾸기 / `items.lang` 을 자연어 컬럼으로 재사용(이미 코드 형식이다) /
번역을 `items` 컬럼에 붙이기(`select('*')` 라 전 방문자 전송량이 두 배가 된다) / snippet 본문 번역(실행되는 명령이다) /
비로그인 방문자에게 즉석 번역 열기(지금 한도 구조로는 셀 수가 없다) / AI 로 분야 영어 이름 자동 확정
